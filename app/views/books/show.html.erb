<div class='container'>
  <div class='row'>
    <div class='col-md-3'>
      <h2>User info</h2>
      <%= render 'users/info', user: @book.user %>
      <h2 class="mt-3">New book</h2>
      <%= render 'form', book: @book_new %>
    </div>
    <div class='col-md-8 offset-md-1'>
  		<h2>Book detail</h2>
  		<table class='table'>
  		  <tr>
  		    <td><%= link_to(@book.user) do %>
  		      <%= image_tag @book.user.get_profile_image, size:"100x100" %><br>
  		      <%= @book.user.name %>
  		      <% end %>
  		    </td>
          <td><%= link_to @book.title, @book %></td>
          <td><%= @book.body %></td>
          <td>コメント数: <%= @book.book_comments.count %></td>
          <td><%= render "favorites/favorite-btn", book: @book %></td>
          <% if @book.user == current_user %>
            <td><%= link_to 'Edit', edit_book_path(@book), class: "btn btn-sm btn-success edit_book_#{@book.id}" %></td>
            <td><%= link_to 'Destroy', @book, method: :delete, data: { confirm: '本当に消しますか？' }, class: "btn btn-sm btn-danger destroy_book_#{@book.id}"%></td>
          <% end %>
        <!--% @comments.each do |comments| %>-->
        <!--  <tr>-->
        <!--    <td>-->
        <!--      %= link_to user_path(comments.user) do %>-->
        <!--        %= image_tag comments.user.get_profile_image, size:"100x100" %><br>-->
        <!--        %= comments.user.name %>-->
        <!--      % end %>-->
        <!--    </td>-->
        <!--    <td>-->
        <!--      %= comments.comment %>-->
        <!--    </td>-->
        <!--    % if comments.user == current_user %>-->
        <!--    <td>-->
        <!--      %= link_to 'Destroy', book_book_comment_path(comments), method: :delete, class: "btn btn-sm btn-danger"%>-->
        <!--    </td>-->
        <!--    % end %>-->
        <!--  </tr>-->
        <!--% end %>-->
        </tr>
      </table>
      <%= render "book_comments/index", book: @book %>
      <%= render "book_comments/form", book: @book, book_comment: @book_comment %>
      <!--%= form_with model: [@book, @book_comment_new] do |f| %>-->
        <!--%= f.text_area :comment, rows:'5', style:"width:100%" %>-->
        <!--%= f.submit '送信' %>-->
      <!--% end %>-->
    </div>
  </div>
</div>